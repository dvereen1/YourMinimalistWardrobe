<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.darianvereen.myapplication2018">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.ClosetActivity" />
        <activity android:name=".Activity.PantsActivity" />
        <activity android:name=".Activity.FinalActivity" />
        <activity android:name=".Activity.ShoesActivity" />
        <activity android:name=".Activity.ShirtsActivity" />
        <activity android:name=".Activity.ViewWardrobe" />

        <receiver
            android:name=".MyReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.darianvereen.myapplication2018"></action>
            </intent-filter>
        </receiver>
    </application>

</manifest>